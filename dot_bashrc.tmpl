# ~/.bashrc — quản lý qua chezmoi, sửa ở dotfiles/dot_bashrc.tmpl rồi chạy `chezmoi apply`

[[ $- != *i* ]] && return   # thoát nếu shell không tương tác

# --- Lịch sử ---
HISTSIZE=5000
HISTFILESIZE=10000
shopt -s histappend
shopt -s checkwinsize

# --- Prompt tối giản: user@host:thư_mục $ ---
PS1='\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$ '

# --- Alias/export dùng chung (source từ file riêng) ---
source "$HOME/.config/shell/aliases.sh"

# --- Alias riêng theo hệ điều hành ---
{{- if eq .role "arch" }}
alias update='sudo pacman -Syu'
alias install='sudo pacman -S'
{{- else }}
alias update='sudo apt update && sudo apt upgrade -y'
alias install='sudo apt install'
{{- end }}


